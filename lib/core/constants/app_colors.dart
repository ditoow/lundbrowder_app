/// App color constants for Lund & Browder Calculator
library;

import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const Color primary = Color(0xFF2196F3);
  static const Color primaryLight = Color(0xFF64B5F6);
  static const Color primaryDark = Color(0xFF1976D2);

  // Background colors
  static const Color background = Color(0xFFF5F5F5);
  static const Color surface = Colors.white;

  // Text colors
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textHint = Color(0xFFBDBDBD);

  // Gradient colors for header
  static const Color gradientStart = Color(0xFF0D3B4C);
  static const Color gradientEnd = Color(0xFF1A5A6E);

  // Severity colors
  static const Color severityRingan = Color(0xFF4CAF50);
  static const Color severitySedang = Color(0xFFFFC107);
  static const Color severityBerat = Color(0xFFF44336);

  // Other colors
  static const Color error = Color(0xFFD32F2F);
  static const Color success = Color(0xFF388E3C);
}
